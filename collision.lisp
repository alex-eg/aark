(defun check-collision (ball blocks &optional board bonuses projectiles)
  ;; check if ball collides with borders
  ;; for block in blocks
  ;; check if collides with ball
  ;; check if board got the bonus
  ;; check if bullets or other projectiles collides with blocks
  ;;
  ;; if any of above happens, also taki in effect all the changes
  ;; the event brings upon us!
  )

(defun check-ball-block (ball block)
  
  )

